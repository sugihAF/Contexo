# Required: shared API key for the CtxHub server (rotate away from "dev-key" before deploy)
CTXHUB_API_KEY=change-me-to-a-real-secret

# Optional: host port to expose (defaults to 8080)
# CTXHUB_PORT=8080
